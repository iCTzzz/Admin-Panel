kind = "web"
previewPath = "/"
title = "MBS Dashboard"
version = "1.0.0"
id = "artifacts/mbs-dashboard"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 18100

[services.development]
run = "pnpm --filter @workspace/mbs-dashboard run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/mbs-dashboard", "run", "build" ]
publicDir = "artifacts/mbs-dashboard/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "18100"
BASE_PATH = "/"
